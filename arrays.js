
const persons = ["Manasseh", "Edwin", 5, true];

// pop method which to remove the last value in the array
persons.pop()

// push method which to add a new value at the end of the array
persons.push("Chidimma")

// shift method which to remove the first value in the array
persons.shift() 

// unshift method which to add a new value at the beginning of the array
persons.unshift("John   ")

console.log(persons)
// console.log(persons[1])
// console.log(persons.length);